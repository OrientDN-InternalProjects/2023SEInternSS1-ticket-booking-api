namespace TicketBooking.Service.Services.SeatService
{
    public interface ISeatService
    {
        // Task<IEnumerable<SeatViewModel>> GetSeatAsync();
        // Task<SeatViewModel> GetSeatAsync(Guid id);
        // Task<int> UpdateSeatAsync(SeatViewModel seatViewModel);
        // Task<int> InsertAsync(SeatViewModel seatViewModel);
        // Task<int> RemoveAsync(Guid id);
    }
}